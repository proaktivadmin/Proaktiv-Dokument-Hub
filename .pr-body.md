## Reports page enhancements

- **Property rows:** Show address + oppdragsnummer instead of raw estate IDs
- **Spelling:** exkl. → eksl. mva. / inkl. mva.
- **Number formatting:** Whole numbers only (no decimals)
- **Live updates:** "Nye cache-events" → "Nye data i Vitec"
- **Best performers:** Independent period selector, scope display

### Migration
- `20260314_0001_report_estate_assignment_number.py` — adds `assignment_number` to `report_sales_estate_cache`

Apply to Railway: `alembic upgrade head` or `run_sql.py`
