{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting Alembic migrations...' && alembic upgrade head --verbose && echo 'Migrations complete! Starting Uvicorn...' && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
