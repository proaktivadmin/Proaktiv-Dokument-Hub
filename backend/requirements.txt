# FastAPI & Server
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22

# Database
sqlalchemy[asyncio]==2.0.46
asyncpg==0.31.0
psycopg2-binary==2.9.9
aiosqlite==0.19.0
alembic==1.18.4

# Data Validation
pydantic==2.12.5
pydantic-settings==2.13.1

# HTTP Client
httpx==0.28.1

# Utilities
python-dateutil==2.8.2
openpyxl>=3.1.5

# Authentication
bcrypt==4.3.0
python-jose[cryptography]==3.4.0

# HTML Processing
beautifulsoup4==4.14.3
lxml==5.4.0
mammoth==1.11.0
striprtf==0.0.29

# Image Processing
Pillow==12.1.1

# Note: WebDAV uses httpx with DigestAuth (no separate webdav library needed)

# Web crawling / scraping (Firecrawl)
firecrawl-py==4.11.1

# AI / LLM (template comparison analysis)
google-generativeai==0.8.6

# Excel export (sales report)
openpyxl==3.1.5

# Timezone data (required on Windows, bundled on Linux but explicit is safer)
tzdata>=2024.1

# Error tracking & performance monitoring
sentry-sdk[fastapi]==2.54.0
