# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy[asyncio]==2.0.46
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.19.0
alembic==1.13.1

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client
httpx==0.26.0

# Utilities
python-dateutil==2.8.2

# Authentication
bcrypt==4.1.2
python-jose[cryptography]==3.3.0

# HTML Processing
beautifulsoup4==4.12.3
lxml==5.1.0
mammoth==1.11.0

# Image Processing
Pillow>=10.0.0

# Note: WebDAV uses httpx with DigestAuth (no separate webdav library needed)

# Web crawling / scraping (Firecrawl)
firecrawl-py==4.11.1

# AI / LLM (template comparison analysis)
google-generativeai==0.8.6

# Error tracking & performance monitoring
sentry-sdk[fastapi]>=2.0.0
