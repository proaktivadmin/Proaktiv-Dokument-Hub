---
phase: 03-social-media-links
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/services/employee_service.py
  - backend/app/routers/employees.py
autonomous: true
---

<objective>
Update backend service and router to handle new social fields.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update employee service</name>
  <files>backend/app/services/employee_service.py</files>
  <action>Add new fields to create() and update() methods. Include in response mapping.</action>
</task>

<task type="auto">
  <name>Task 2: Update employee router</name>
  <files>backend/app/routers/employees.py</files>
  <action>Ensure router passes new fields through. Add optional is_featured filter to list endpoint.</action>
</task>

<task type="auto">
  <name>Task 3: Test endpoint</name>
  <files>-</files>
  <action>Verify GET /api/employees returns new fields, POST/PUT accept them.</action>
</task>
</tasks>

<success_criteria>
- GET /api/employees returns social fields and is_featured_broker
- PUT /api/employees/{id} accepts social URL updates
- Filter: GET /api/employees?is_featured=true works
</success_criteria>
