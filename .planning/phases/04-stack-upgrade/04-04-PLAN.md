---
phase: 04-stack-upgrade
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - frontend/src/**/*.tsx
autonomous: true
---

<objective>
Full build verification and fix any remaining issues.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: TypeScript verification</name>
  <files>-</files>
  <action>Run npx tsc --noEmit to find all type errors.</action>
</task>

<task type="auto">
  <name>Task 2: Fix type errors</name>
  <files>varies</files>
  <action>Address any remaining TypeScript errors from upgrades.</action>
</task>

<task type="auto">
  <name>Task 3: Lint check</name>
  <files>-</files>
  <action>Run npm run lint and fix any new warnings/errors.</action>
</task>

<task type="auto">
  <name>Task 4: Production build</name>
  <files>-</files>
  <action>Run npm run build and ensure it completes successfully.</action>
</task>

<task type="auto">
  <name>Task 5: Local smoke test</name>
  <files>-</files>
  <action>
    Run npm run dev and manually verify:
    - Dashboard loads
    - Login works
    - Template list renders
    - Styles look correct
  </action>
</task>
</tasks>

<success_criteria>
- Zero TypeScript errors
- Zero lint errors (or only pre-existing)
- Production build succeeds
- Local dev works
</success_criteria>
