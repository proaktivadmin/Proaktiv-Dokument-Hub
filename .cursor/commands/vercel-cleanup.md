# Vercel Cleanup

Execute the phase 5 cleanup plan as a thin orchestration command.

## Execute

1. Read `.cursor/context-registry.md`.
2. Confirm QA passed via `/vercel-qa`.
3. Execute `.planning/phases/05-vercel-migration/05-05-PLAN.md`.

## Guardrail

Do not run destructive cleanup steps unless QA has explicitly passed and the user confirms.
