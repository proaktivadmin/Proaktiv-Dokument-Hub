# Vercel QA

Execute the phase 5 verification plan as a thin orchestration command.

## Execute

1. Read `.cursor/context-registry.md`.
2. Read `.planning/phases/05-vercel-migration/05-04-PLAN.md`.
3. Use `.cursor/agents/QA_MASTER.md` reporting style for pass/fail output.

## Next Step

- If passing: invoke `/vercel-cleanup`.
- If failing: return findings to `/vercel-builder`.
