<style>
/* vitec-structure.css */
/* Core Layout & Resets - UNTOUCHED BY THEMES */

html, body {
    margin: 0;
    padding: 0;
    height: 100%;
}

/* Vitec Variables List Table Structure */
#vitecTemplate table.vitec-variables-list {
    table-layout: fixed;
    width: 100%;
    border-collapse: collapse;
}

#vitecTemplate table.vitec-variables-list th {
    text-align: left;
    padding: 0 5px;
}

#vitecTemplate table.vitec-variables-list td {
    padding: 0;
    vertical-align: top;
    word-wrap: break-word;
    /* Border color handled by theme */
    border-top: 1px solid var(--border-color, #ededed); 
}

#vitecTemplate table.vitec-variables-list tr:first-child td {
    border-top: none;
}

/* Headers within the list */
#vitecTemplate table.vitec-variables-list td h3 {
    margin: 1em 0 0 0;
    padding: 0;
    font-size: var(--font-size-h3, 16pt);
    color: var(--color-primary, darkblue);
}

#vitecTemplate table.vitec-variables-list td h3 > small {
    font-weight: normal;
    white-space: nowrap;
    margin-left: 20px;
    font-size: 0.8em;
}

#vitecTemplate > table.vitec-variables-list td {
    padding: var(--spacing-sm, 5px);
}

/* RESET: Prevent global borders on generic tables within Vitec Template */
#vitecTemplate table:not(.vitec-variables-list):not(.form-table) td,
#vitecTemplate table:not(.vitec-variables-list):not(.form-table) th {
    border: none !important;
}

/* Images & Figures */
#vitecTemplate figure {
    text-align: center;
    display: inline-block;
    margin: 0;
}

#vitecTemplate figure img:not(.scaled-image) {
    max-width: 3cm;
    max-height: 3cm;
}

#vitecTemplate figure figcaption {
    font-size: smaller;
    display: block;
}

#vitecTemplate .scaled-image {
    width: 100px;
    height: 100px;
    object-fit: cover;
    object-position: center center;
    border-radius: var(--border-radius-circle, 50%);
}

/* Form Tables (if used) */
#vitecTemplate .form-table {
    border-top: solid 1px var(--color-text, #000);
}

#vitecTemplate .form-table td {
    border: solid 1px var(--color-text, #000);
}

#vitecTemplate .form-table td p {
    margin: 0;
    padding: 0;
}

/* Data Labels (for inputs) */
#vitecTemplate table td[data-label] {
    padding: 16px 2px 5px 4px;
    position: relative;
}

#vitecTemplate table td[data-label]:before {
    content: attr(data-label);
    text-align: left;
    font-size: 9pt;
    line-height: 8pt;
    display: block;
    position: absolute;
    top: 5px;
    color: var(--color-text-secondary, #666);
}

/* Version Toggles & Interactive Elements */
#vitecTemplate #versionToggleButton {
    display: none;
}

#vitecTemplate label[for=versionToggleButton] {
    cursor: pointer;
}

/* SVG Toggles (Checkbox/Radio) */
.svg-toggle {
    display: inline-block !important;
    width: 22px;
    height: 22px;
    margin: 0 5px;
    vertical-align: bottom;
    background-repeat: no-repeat;
    background-size: 22px 22px;
    background-position: center center;
}

/* Version Highlights */
#vitecTemplate [data-version]:after {
    content: attr(data-version);
    display: table-cell;
    white-space: nowrap;
    float: right;
    padding: 1px 4px;
    color: #fff;
    font-size: 13px;
    margin: 4px;
    background-color: rgb(4 98 175 / 0.6); /* Default blue-ish */
    border-radius: 3px;
}

/* theme-base.css */
/* Base Design Tokens - Defaults */

:root {
    /* Colors */
    --color-primary: #000080;
    /* Classic Navy */
    --color-text: #000000;
    /* Black */
    --color-text-secondary: #666666;

    --color-bg-page: #ffffff;
    --color-bg-secondary: #f7f7f7;

    --border-color: #ededed;
    --border-color-dark: #000000;

    /* Typography */
    --font-family-base: sans-serif;
    --font-size-base: 10pt;
    --font-size-h1: 20pt;
    --font-size-h3: 16pt;

    /* Spacing */
    --spacing-xs: 2px;
    --spacing-sm: 5px;
    --spacing-md: 10px;
    --spacing-lg: 20px;

    /* Shape */
    --border-radius-sm: 4px;
    --border-radius-circle: 50%;
}

body {
    font-family: var(--font-family-base);
    font-size: var(--font-size-base);
    color: var(--color-text);
    background-color: var(--color-bg-page);
}

h1,
h2,
h3,
h4,
h5,
h6 {
    color: var(--color-primary);
}

/* Proaktiv Design Tokens - Premium & Classic */

:root {
    /* Brand Colors - Official PG Manual */
    --color-primary: #272630;
    /* PG - MØRK BLÅ (Charcoal/Navy) */
    --color-primary-light: #3E3D4A;

    --color-pg-beige: #e9e7dc;
    /* PG - BEIGE (Official) */
    --color-pg-gronn: #a4b5a8;
    /* PG - GRØNN (Muted Green) */
    --color-pg-bronse: #bcab8a;
    /* PG - BRONSE (Muted Gold/Beige) */

    --color-accent-gold: var(--color-pg-bronse);
    /* Map Gold to Bronze */
    --color-bg-beige: var(--color-pg-beige);
    /* Map warm background to Beige */

    --color-text: #1d1d1d;
    /* PG - SORT (Soft Black for text) */
    --color-text-secondary: #5e6d82;

    --color-bg-page: #ffffff;
    /* HVIT */
    --color-bg-secondary: var(--color-pg-beige);

    --border-color: #D6D4CD;
    --border-color-dark: #272630;

    /* Typography - Premium Mix */
    /* Import Playfair if available, fallback to Times */
    --font-family-base: 'Arial', 'Helvetica', sans-serif;
    --font-family-serif: 'Playfair Display', 'Times New Roman', serif;
    /* Manual: Bolignavn is Playfair */

    --font-size-base: 10pt;
    --font-size-h1: 24pt;
    --font-size-h3: 14pt;

    /* Spacing - Generous */
    --spacing-xs: 4px;
    --spacing-sm: 8px;
    --spacing-md: 16px;
    --spacing-lg: 32px;
    --spacing-xl: 48px;

    /* Shape - Sharp & Boxy */
    --border-radius-sm: 0px;
    --border-radius-circle: 0px;
}

/* Specific Overrides for Proaktiv Look */

/* Make headers uppercase/tracking for premium feel */
#vitecTemplate table.vitec-variables-list td h3 {
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 600;
    font-size: 11pt;
    font-family: var(--font-family-base);
    border-bottom: 2px solid var(--color-primary);
    padding-bottom: 4px;
    margin-bottom: 12px;
    display: inline-block;
}

#vitecTemplate table.vitec-variables-list td h3>small {
    display: none;
}

/* Quote Construction (Gold Quote Mark) */
.proaktiv-quote {
    font-family: var(--font-family-serif);
    font-size: 16pt;
    font-style: italic;
    color: var(--color-primary);
    position: relative;
    padding-left: 20px;
}

.proaktiv-quote::before {
    content: '"';
    font-family: var(--font-family-serif);
    font-size: 60pt;
    color: var(--color-pg-bronse);
    position: absolute;
    top: -20px;
    left: -10px;
    z-index: -1;
    opacity: 0.5;
}

/* Dividers */
.proaktiv-divider-bronse {
    border-bottom: 1px solid var(--color-pg-bronse);
    width: 50px;
    margin: 10px 0;
}

/* The Lily - Brand Icon */
.proaktiv-lily-icon {
    display: inline-block;
    width: 40px;
    height: 48px;
    background-image: url('../assets/proaktiv-lily-black.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.8;
}

.proaktiv-lily-watermark {
    position: absolute;
    bottom: 40px;
    right: 40px;
    width: 60px;
    height: 72px;
    background-image: url('../assets/proaktiv-lily-black.png');
    background-size: contain;
    background-repeat: no-repeat;
    opacity: 0.1;
}

/* Logos */
.proaktiv-logo-gold {
    width: 200px;
    background-image: url('../assets/proaktiv-logo-gold.png');
    background-size: contain;
    background-repeat: no-repeat;
    height: 50px;
    display: block;
}

.proaktiv-logo-black {
    width: 200px;
    background-image: url('../assets/proaktiv-logo-black.png');
    background-size: contain;
    background-repeat: no-repeat;
    height: 50px;
    display: block;
}

/* Video Overlay Style (Corner Accents / L-Bracket) */
/* The "L" is top-right, made of two lines */
.proaktiv-video-card-dark {
    background-color: var(--color-primary);
    color: white;
    padding: var(--spacing-lg);
    position: relative;
    overflow: hidden;
}

.proaktiv-video-card-dark::after {
    /* L-Corner Accent (Grey/Silver) */
    content: '';
    position: absolute;
    top: 20px;
    right: 20px;
    width: 40px;
    height: 40px;
    border-top: 4px solid #D6D4CD;
    border-right: 4px solid #D6D4CD;
    z-index: 10;
}

.proaktiv-video-card-light {
    background-color: var(--color-pg-beige);
    color: var(--color-primary);
    padding: var(--spacing-lg);
    position: relative;
    overflow: hidden;
}

.proaktiv-video-card-light::after {
    /* L-Corner Accent (White) */
    content: '';
    position: absolute;
    top: 20px;
    right: 20px;
    width: 40px;
    height: 40px;
    border-top: 4px solid white;
    border-right: 4px solid white;
    z-index: 10;
}

/* --- Global Element Styles (Applied to #vitecTemplate) --- */
/* This ensures that when we strip inline styles from Legacy documents, they inherit the Premium look. */

#vitecTemplate h1,
h1 {
    font-family: var(--font-family-serif);
    font-size: var(--font-size-h1);
    font-weight: 700;
    color: var(--color-primary);
    text-transform: uppercase;
    border-bottom: 3px solid var(--color-pg-bronse);
    padding-bottom: var(--spacing-sm);
    margin-bottom: var(--spacing-lg);
    page-break-after: avoid;
}

#vitecTemplate h2,
h2 {
    font-family: var(--font-family-serif);
    font-size: var(--font-size-h3);
    color: var(--color-primary);
    background-color: var(--color-bg-secondary);
    /* Beige background strip */
    border-left: 5px solid var(--color-pg-bronse);
    padding: var(--spacing-sm) 0 var(--spacing-sm) var(--spacing-md);
    margin-top: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
    page-break-after: avoid;
}

#vitecTemplate p,
p {
    margin-bottom: var(--spacing-md);
    line-height: 1.5;
}

#vitecTemplate a,
a {
    color: var(--color-primary);
    text-decoration: underline;
}

/* Utility Helper for Signature Blocks */
.proaktiv-signature-block {
    border-top: 3px solid var(--color-pg-bronse);
    padding-top: var(--spacing-lg);
    page-break-inside: avoid;
    margin-top: var(--spacing-xl);
}
</style>
